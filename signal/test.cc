#include <stdio.h>
#include <unistd.h>
#include <signal.h>

//kill
int main()
{
    // C 语言字符串转整数
    // 1. atoi
    // char str[] = "100";
    // int num = atoi(str);
    // printf("%d\n",num);

    //2. sprintf/sscanf
    // char str[] = "100";
    // int num = 0;
    // sscanf(str, "%d", &num);
    // printf("%d\n",num);

    
    return 0;
}